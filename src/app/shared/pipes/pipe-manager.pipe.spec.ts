import { PipeManagerPipe } from './pipe-manager.pipe';

describe('PipeManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
